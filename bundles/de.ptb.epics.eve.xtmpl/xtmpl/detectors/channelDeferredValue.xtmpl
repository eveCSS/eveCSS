            <!-- channel without unit, to be included by detetctors, reading PV as deferred without .VAL -->
            <channel deferred="true" saveValue="false">
                <class>${Ch_Class=""}</class>
                <name>${Name=""}</name>
                <id>${PV}chan1</id>
                include "channel_read_ohne_val_part.xtmpl"
            </channel>

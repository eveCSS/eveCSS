        <!-- basic detector (must include a channel) without unit-->
        <detector>
            <class>${Class=""}</class>
            <name>${Name=""}</name>
            <id>${PV}basic</id>
            include "channelNoUnit.xtmpl"
        </detector>


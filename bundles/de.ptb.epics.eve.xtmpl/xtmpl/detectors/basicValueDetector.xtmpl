        <!-- basic detector (must include a channel) without unit, read only the value of the PV-->
        <detector>
            <class>${Class=""}</class>
            <name>${Name=""}</name>
            <id>${PV}value</id>
            include "channelValue.xtmpl"
        </detector>


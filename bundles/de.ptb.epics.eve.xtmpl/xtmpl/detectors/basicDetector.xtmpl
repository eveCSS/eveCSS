        <!-- basic detector (must include a channel) -->
        <detector>
            <class>${Class=""}</class>
            <name>${Name=""}</name>
            <id>${PV}basic</id>
            include "channel.xtmpl"
        </detector>

        <!-- basic detector (must include a channel) with more options -->
        <detector>
            <class>${Class=""}</class>
            <name>${Name=""}</name>
            <id>${PV}basic</id>
            include "channel.xtmpl"
            include "channel_spec_options_part.xtmpl"
        </detector>
